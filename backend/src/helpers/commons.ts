import path from "path";

export const projectPath = path.resolve(__dirname, "..", "..", "..", "..", "..", "..", "..");
export const backPath = path.resolve(projectPath, "back");
export const apiPath = path.resolve(backPath, "api");
